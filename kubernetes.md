# What is Kubernetes?

## Components

* A *cluster* is a set of nodes (worker machines - virtual or physical) that run containerized applications.
* A *context* is an element of a kubeconfig file and is used to group access parameters under a convenient name. A
  context has 3 components:
    1. cluster
    2. namespace
    3. user

